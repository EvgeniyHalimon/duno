import React from "react";
import { Box, Button } from '@mui/material';

export const Switcher: React.FC = () => {
    return(
        <Box>
            <Button>Anime</Button>
            <Button>Manga</Button>
        </Box>
    )
}